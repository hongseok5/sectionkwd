<!DOCTYPE html>
<html>
  <head>
    <link rel='stylesheet' href='/Bootstrap-3.3.7/css/bootstrap.css' />
    <script type="text/javascript" src="/jQuery-3.3.1/jquery-3.3.1.js"></script>
    <link href="https://unpkg.com/tabulator-tables@4.7.1/dist/css/tabulator.min.css" rel="stylesheet">
    <script type="text/javascript" src="https://unpkg.com/tabulator-tables@4.7.1/dist/js/tabulator.min.js"></script>

    <script>

      // table.setData로 처리하는 것도 한 방법일 수도...
      // table.setData(URL) 이런식으로 아쟉스 요청 가능
      // table.setData() 이러면 리로드 됨.
      $(document).ready( function(){

        var ctable = new Tabulator("#tb_category2", {
          ajaxURL : "http://localhost:3000/typos/getTableData",
          ajaxParams : { size : "1" },
          ajaxConfig : "POST",
          layout : "fitDataFill",
          ajaxContentType:"json",
          autoColumns : true
        })
        console.log(ctable)


      });

    </script>
  </head>
  <body>
    <div class="row">
      <div class="col-md-2">
        <h4 style="margin-top: 30px;"> 대분류 </h4>
        <ul style="margin-top: 30px;">
      
          <% for(let i = 0; i < data1.cate1s.length; i++){ %>
            <li><%= data1.cate1s[i].category1 %> ( <%= data1.cate1s[i].count %> )</li>
          <% } %>
        </ul>
      </div>
      <div class="col-md-10">
        <p>키워드목록</p>
        <!--
        <table id="sctable">

          <thead>
            <tr>
                        
              <th class="center">중분류 </th>
              <th class="center">키워드 수 </th>
                        
            </tr>
          </thead>

          <tbody>
          </tbody>
        </table>
        -->
        <div id="tb_category2">

        </div>
      </div>
    </div>

  </body>
</html>
